# free-Polygon-Area-Calculator
Solution for Polygon Area Calculator

## Uso

Ejecute el script con Python para ver pruebas rápidas:

```bash
python3 polygon_area_calculator.py
```

La implementación incluye las clases `Rectangle` y `Square` en `polygon_area_calculator.py`.
